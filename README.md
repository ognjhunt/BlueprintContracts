# BlueprintContracts

Shared contract package for portable artifacts exchanged between Blueprint repos.

This package is intentionally lightweight and stdlib-only. It owns:

- qualified opportunity handoff validation
- site-world bundle loading and structural validation
- runtime-layer grounding policy schemas and thresholds
- canonical package version computation and verification
